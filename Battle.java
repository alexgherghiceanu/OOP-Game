package org.example.entities.characters;

//Interface that serves as the base for all the characters
public interface Battle {
    public void receiveDamage(int damage);
    public int getDamage();
}
